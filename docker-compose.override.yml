# Development overrides for docker-compose.yml.
# Only loaded by `docker compose` (not `docker-compose` v1).

services:
  postgres:
    ports:
      - "5432:5432"
    environment:
      POSTGRES_LOG_STATEMENT: all
